# 项目0 Week2：做网页应用 + 上传 GitHub

## 本周目标

把训练好的模型变成一个网页：上传照片 → 显示情绪识别结果。
然后写 README，上传 GitHub，让别人也能看到你的项目。

---

## 文件结构（本周新增）

```
multimodal-ai-learning/
├── data/                   ← 已有
├── train.ipynb             ← 已有（Week1）
├── predict.ipynb           ← 已有（Week1）
├── emotion_model.pth       ← 已有（Week1 训练生成）
├── app.py                  ← 【本周新建】网页应用
└── README.md               ← 【本周新建】项目说明文档
```

**本周要创建的文件：`app.py` 和 `README.md`**

注意：网页应用用 `.py` 文件，不用 `.ipynb`。
因为最终运行方式是在命令行输入 `python app.py`，不是在 Jupyter 里跑。

---

## Step 1：安装 Gradio

在 Anaconda Prompt 中运行：

```bash
conda activate multimodal
pip install gradio
```

安装完后，先测试 Gradio 能不能用。
新建一个 `test_gradio.py` 文件（测完可以删掉），写入：

```python
import gradio as gr

def greet(name):
    return f"你好, {name}!"

demo = gr.Interface(fn=greet, inputs="text", outputs="text")
demo.launch()
```

运行：

```bash
python test_gradio.py
```

**浏览器会自动打开一个网页，输入名字点提交，能看到回复就说明安装成功。**
按 Ctrl+C 关闭。

---

## Step 2：创建 app.py

**Gradio 的工作原理很简单：**
1. 你定义一个函数：输入是图片，输出是预测结果
2. Gradio 自动帮你生成网页界面
3. 用户上传图片 → Gradio 调用你的函数 → 显示结果

用记事本或 VSCode 新建 `app.py`，写入以下完整代码：

```python
import gradio as gr
import torch
import torch.nn as nn
from torchvision import models, transforms

# ============================================================
# 第1部分：加载模型
# 和 predict.ipynb 里一样，加载训练好的模型
# ============================================================

model = models.resnet18()
model.fc = nn.Linear(512, 7)
model.load_state_dict(torch.load('emotion_model.pth'))
model.eval()

device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
model = model.to(device)

# 预处理（和训练时一致）
transform = transforms.Compose([
    transforms.Resize((48, 48)),
    transforms.Grayscale(num_output_channels=3),
    transforms.ToTensor(),
    transforms.Normalize([0.5, 0.5, 0.5], [0.5, 0.5, 0.5])
])

print("模型加载完成")

# ============================================================
# 第2部分：定义预测函数
# Gradio 会把用户上传的图片传给这个函数
# 函数返回一个字典：{情绪名: 概率}，Gradio 自动显示成柱状图
# ============================================================

def predict_emotion(image):
    """
    输入：PIL 图片（Gradio 自动把上传的图片转成 PIL 格式）
    输出：字典，key 是情绪名，value 是概率
    """
    img_tensor = transform(image).unsqueeze(0).to(device)

    with torch.no_grad():
        output = model(img_tensor)
        probs = torch.softmax(output, dim=1)[0]

    # 7种情绪（按字母顺序，和训练时一致）
    emotions = ['😠 生气', '🤢 厌恶', '😨 害怕', '😊 开心', '😐 平静', '😢 难过', '😲 惊讶']

    return {emotions[i]: float(probs[i]) for i in range(7)}

# ============================================================
# 第3部分：创建网页界面
# gr.Interface 是最简单的方式：指定函数、输入类型、输出类型
# ============================================================

demo = gr.Interface(
    fn=predict_emotion,                              # 调用哪个函数
    inputs=gr.Image(type="pil", label="上传人脸照片"),  # 输入：图片
    outputs=gr.Label(label="情绪识别结果", num_top_classes=7),  # 输出：标签（显示概率）
    title="😊 人脸情绪识别系统",
    description="上传一张人脸照片，AI 自动识别情绪（7种：开心、难过、生气、惊讶、害怕、厌恶、平静）",
    examples=[                                       # 示例图片（可选，方便演示）
        ["./data/emotion/val/happy/happy_1.jpg"],
        ["./data/emotion/val/sad/sadshushu.jpg"],
    ]
)

# ============================================================
# 第4部分：启动
# ============================================================

if __name__ == "__main__":
    demo.launch()
```

**运行方式：**

```bash
cd C:\Users\29208\Desktop\规划\项目学习\multimodal-ai-learning
conda activate multimodal
python app.py
```

**运行后：**
1. 终端显示一个网址（通常是 `http://127.0.0.1:7860`）
2. 浏览器自动打开
3. 你可以上传图片测试
4. 关闭时在终端按 Ctrl+C

**注意：** examples 里的图片路径要确保存在，如果文件名不对，把那几行删掉就行，不影响功能。

---

## Step 3：写 README.md

用记事本或 VSCode 新建 `README.md`，写入：

```markdown
# 😊 人脸情绪识别系统

## 项目介绍

使用深度学习识别人脸照片中的情绪表情。上传一张人脸照片，自动输出情绪类别和概率。

## 支持的情绪（7种）

| 情绪 | 英文 |
|------|------|
| 😊 开心 | Happy |
| 😢 难过 | Sad |
| 😠 生气 | Angry |
| 😐 平静 | Neutral |
| 😨 害怕 | Fear |
| 🤢 厌恶 | Disgust |
| 😲 惊讶 | Surprise |

## 技术栈

- **模型**：ResNet18（ImageNet 预训练 + 微调）
- **框架**：PyTorch 2.5
- **数据集**：FER2013（约28000张训练，7000张验证）
- **界面**：Gradio
- **加速**：CUDA（GPU）

## 训练效果

- 最佳验证准确率：62.51%
- 训练轮数：10 Epoch
- FER2013 人类标注准确率约 65%，模型表现接近人类水平

## 安装

```bash
conda activate multimodal
pip install torch torchvision gradio
```

## 使用

```bash
python app.py
```

浏览器自动打开，上传人脸照片即可识别。

## 项目结构

```
├── train.ipynb         # 训练代码
├── predict.ipynb       # 测试代码
├── app.py              # 网页应用
├── emotion_model.pth   # 训练好的模型
├── README.md           # 项目说明
└── data/emotion/       # 数据集
```

## 作者

[你的名字]
```

---

## Step 4：上传 GitHub

### 4.1 创建 GitHub 仓库

1. 打开 https://github.com/，登录（没账号先注册）
2. 点击右上角 `+` → `New repository`
3. 名称填：`emotion-recognition`
4. 描述填：`Face emotion recognition using deep learning`
5. 选 Public（公开）
6. **不要**勾选 "Add a README file"（我们已经写了）
7. 点击 `Create repository`

### 4.2 上传代码

在 Anaconda Prompt 中运行：

```bash
cd C:\Users\29208\Desktop\规划\项目学习\multimodal-ai-learning

# 初始化 Git
git init

# 添加所有文件（但不要上传数据集和模型文件，太大了）
# 先创建 .gitignore 文件，告诉 Git 忽略哪些文件
echo data/ > .gitignore
echo *.pth >> .gitignore
echo __pycache__/ >> .gitignore
echo .ipynb_checkpoints/ >> .gitignore

# 添加文件
git add .

# 提交
git commit -m "人脸情绪识别系统：ResNet18 + Gradio"

# 连接 GitHub（把"你的用户名"改成你的 GitHub 用户名）
git remote add origin https://github.com/你的用户名/emotion-recognition.git
git branch -M main
git push -u origin main
```

**上传成功后，打开你的 GitHub 仓库页面就能看到代码了。**

**为什么不上传 data/ 和 .pth？**
- 数据集几百MB，GitHub 有文件大小限制
- 别人可以自己下载数据集，按你的代码训练
- 模型文件也很大，可以在 README 里说明如何训练获得

---

## Week 2 完成标志

- [ ] Gradio 安装成功，测试页面能打开
- [ ] `app.py` 运行成功，能上传图片识别情绪
- [ ] `README.md` 写好，数据是真实的
- [ ] GitHub 仓库创建成功，代码已上传

---

## 项目0 全部完成

做到这里，你有了：
- 一个训练好的情绪识别模型
- 一个可以演示的网页应用
- 一个 GitHub 仓库

**下一步：项目1（图像分类系统），从2类扩展到10+类。**
